{
  "name": "exam02",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'exam1' (`git checkout -b exam2`)\r * modify the `questions.md` file to have the answers required\r * Add any files necessary to create the application below\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the TAs and I as reviewers.  \r * Due by 11:59pm Sun Mar 24",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "nodemon": "^1.18.10"
  }
}
